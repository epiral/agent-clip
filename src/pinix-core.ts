// @ts-nocheck

const coreModule = "@pinixai/core";
const core = await import(coreModule);

export const Clip = core.Clip;
export const command = core.command;
export const handler = core.handler;
export const z = core.z;
